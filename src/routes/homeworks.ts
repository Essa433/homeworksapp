import { FastifyInstance } from 'fastify';

export default async function (server: FastifyInstance) {
	server.get('/essa', async (request: any, reply: any) => {
		// add your code here
	});

	server.get('/essa/Homework', async (request: any, reply: any) => {
		// add your code here
	});
}
